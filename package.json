{
  "name": "northkeep-monorepo",
  "private": true,
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "test:e2e": "pnpm build && vitest run --config e2e/vitest.config.ts",
    "northkeep": "node packages/cli/dist/index.js",
    "tauri": "pnpm --dir apps/desktop tauri"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.17.0",
    "@northkeep/core": "workspace:*",
    "@northkeep/platform-node": "workspace:*",
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
