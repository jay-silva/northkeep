# Expo / React Native local artifacts
.expo/
dist/
web-build/
expo-env.d.ts

# Native projects are generated by prebuild/EAS; not checked in for M6-1
# The GENERATED native project (CNG/prebuild). Local expo modules keep their
# own ios/ sources, which must stay tracked or EAS never sees them (build 16:
# "Cannot find native module" because modules/pdf-text/ios was ignored).
/ios/
!modules/**/ios/
android/

# Metro
.metro-health-check*
