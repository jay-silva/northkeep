{
  "name": "@northkeep/connector-server",
  "version": "0.1.0",
  "description": "NorthKeep hosted shareable-scope connector: a Neon-backed MCP connector service. Serves ONLY scopes the user explicitly marked Shared, to the AI apps they already use. OAuth 2.1 authorization server with ALL state persisted (invariant: any serverless instance can serve any request).",
  "license": "AGPL-3.0-only",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "@neondatabase/serverless": "^0.10.0",
    "@noble/ciphers": "^2.2.0",
    "@noble/hashes": "^2.2.0",
    "express": "^4.21.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@northkeep/core": "workspace:*",
    "@northkeep/platform-node": "workspace:*",
    "@northkeep/sync": "workspace:*",
    "@types/express": "^4.17.21"
  }
}
